<main class="dashboard-content">
    <h2>Expense Dashboard</h2>

    <div class="dashboard-summary">
        <div class="summary-card">
            <h3>Total Expenses</h3>
            <p class="total-amount">$<%= parseFloat(totalExpenses).toFixed(2) %></p>
        </div>
    </div>

    <div class="dashboard-chart">
        <canvas id="expensePieChart" width="400" height="400"></canvas>
        <!--<% if (categoryData.length > 0) { %>
            <ul class="chart-legend">
                <% categoryData.forEach(item => { %>
                    <li><span class="category-color" data-color="<%= item.color %>"></span> <%= item.category %>: $<%= parseFloat(item.total).toFixed(2) %></li>
                <% }); %>
            </ul>-->
        <% } else { %>
            <p>No expense data available for category analysis.</p>
        <% } %>
    </div>
</main>